import 'package:flutter/material.dart';

class DetailedHabitPage extends StatelessWidget {
  const DetailedHabitPage({Key? key}) : super(key: key);

  static const routeName = '/detailed_habit';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
